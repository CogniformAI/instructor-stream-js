# Example environment variables for act (local GitHub Actions testing)
# Copy this to .env and modify as needed
#
# Note: .env is gitignored

# CI environment simulation
CI=true
RUN_PROVIDER_TESTS=false
RUN_BENCHMARK_TESTS=false

# Node environment
NODE_ENV=test

# GitHub repository info (for act)
GITHUB_REPOSITORY=cogniformai/instructor-stream-js
GITHUB_REF=refs/heads/main