#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Temporarily disabled during Island AI packages integration
# bun run lint
# bun run type-check

echo "Pre-commit hooks temporarily disabled during development"

